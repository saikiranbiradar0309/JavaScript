// The + operator can also be used to concatenate strings in JavaScript.
// The + operator can also be used to concatenate strings in JavaScript. When used with strings, it combines them into a single string.

let firstName = "Saikiran";
let lastName = "Biradar";

console.log(firstName + " " + lastName);

// IMPORTANT: 
console.log(10 + 20);
// Output: 30 (Addition of numbers)

console.log(10 + "20");
// Output: "1020" (Concatenation of strings)